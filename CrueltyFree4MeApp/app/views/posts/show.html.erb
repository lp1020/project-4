 <div class="postShowDiv">
    <h4 class="postName"><%= @post.name %></h4>

    <p class="postCatShow"><%= @category.name %></p>

    <p class="postInfoShow">Cruelty Free?<p id="postBooleanShow"> <%= @post.is_cruelty_free.to_s.capitalize %></p></p>
    <a class="brandLinkShow"href=<%= @post.link %>>Go to <%= @post.name %> Homepage</a>
    <p class="postInfoShow">Posted at: <%= @post.created_at.to_time.in_time_zone("Eastern Time (US & Canada)").strftime('%B %e at %l:%M %p')%></p>
    <p class="postInfoShow">Updated at: <%= @post.updated_at.to_time.in_time_zone("Eastern Time (US & Canada)").strftime('%B %e at %l:%M %p')%></p>
    </div>
  <div class="linkName"><%= link_to 'Edit this Entry!', edit_post_path %></div>

<div class="linkName"><%= link_to 'Delete this Entry!', post_path(@post), method: :delete,
  data: { confirm: "Are you sure you want to delete #{@post.name}?" } %></div>
